export type AblationDatum = {
  mode: string;
  unique_combo_count: number;
  combo_entropy: number;
  predictability_rate: number;
  mean_abs_key_corr: number;
  mean_hamming_rate: number;
  mean_total_ms: number;
};